legacy-peer-deps=true
# Avoid audit bulk fetch hanging/failing when TLS inspection breaks registry.npmjs.org
audit=false
fund=false
# Use Windows certificate store (fixes UNABLE_TO_VERIFY_LEAF_SIGNATURE on many networks)
node-options=--use-system-ca

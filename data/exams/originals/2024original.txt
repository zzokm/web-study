Course Name: Web Technology
Course Code: IS231
Date: 06-June-2024
Instructor(s): Dr. Neamat El-Tazi & Dr. Laila Esheiba
Exam Duration: 2 Hours
Total Marks: 60

Final Exam

PLEASE DELIVER THE EXAM PAPER WITH THE BUBBLE SHEET.
For each of the following statements, choose the right answer and copy your answer in the bubble sheet:

1. Django is based on which framework?
a. MVC
b. MVVM
c. MVT Model View Template
d. None of the previous
ANSWER: C
EXPLANATION: Django uses the MVT (Model-View-Template) pattern, matching option c.

2. For a model to be visible in the admin interface it has to be registered in which file?
a. settings.py
b. admin.py using admin.site.register(Model_name)
c. views.py
d. None of the previous
ANSWER: B
EXPLANATION: Models are registered using `admin.site.register()` within the `admin.py` file of a Django app to make them accessible via the Django admin panel.

3. Which of the following statement(s) is true about the JavaScript?
a. It is a scripting language used to make the website interactive
b. It is an advanced version of Java for Desktop and Mobile application development
c. It is a markup language of Java to develop the webpages
d. All of the above
ANSWER: A
EXPLANATION: JavaScript is a lightweight, interpreted scripting language primarily known for adding interactivity to web pages. It is entirely distinct from Java.

4. To perform programming logic in Django templates, you can use 'template tags', what is a correct syntax to start a 'for' loop?
a. (for x in y)
b. <% for x in y %>
c. {% for x in y %}
d. None of the previous
ANSWER: C
EXPLANATION: Django template tags use `{% %}`. A `for` loop starts with `{% for x in y %}`. Python-style parentheses, ASP-style `<% %>`, and "none of the previous" are not valid Django template syntax.

5. What is a correct syntax for creating a Django project?
a. django start my_tennis_club
b. py manage.py start-django my_tennis_club
c. django-admin startproject my_tennis_club
d. None of the previous
ANSWER: C
EXPLANATION: The standard CLI command to initialize a new Django project is `django-admin startproject <projectname>`.

6. If you have a Django application named members, and a model named Member, what is a correct syntax to import the model?
a. from members.models import Member
b. import Member from members
c. import members.Member
d. None of the previous
ANSWER: A
EXPLANATION: In Python and Django, models are stored in the `models.py` file within the app directory. The correct relative import is `from <appname>.models import <ModelName>`.

7. Which is the correct syntax to call an external JavaScript file in the current HTML document?
a. <script src="jsfile.js"></script>
b. <script href=" jsfile.js"></script>
c. <import src=" jsfile.js"></import>
d. <script link=" jsfile.js"></script>
ANSWER: A
EXPLANATION: External JavaScript files are linked using the HTML `<script>` tag with the `src` (source) attribute pointing to the file path.

8. Which JavaScript method is used to access an HTML element by id?
a. getElementById()
b. getElement(id)
c. getElementById(id)
d. elementById(id)
ANSWER: C
EXPLANATION: While 'a' represents the raw method name, 'c' explicitly shows the method signature with the required parameter, which is typically the expected answer in academic contexts. Technically, it is a method of the `document` object.

9. Which property is used to define the HTML content to an HTML element with a specific id?
a. innerText
b. innerContent
c. element Text
d. innerHTML
ANSWER: D
EXPLANATION: The `innerHTML` property sets or returns the HTML content inside an element.

10. When you have done changes in a model in Django, which command has to be executed in order to make the changes take effect in the database?
a. python manage.py runmigrations
b. python manage.py executemigrations
c. python manage.py makemigrations
d. None of the previous
ANSWER: D
EXPLANATION: `makemigrations` only creates migration files; applying them requires `migrate`, which is not listed.

11. Which JavaScript method is used to write on browser's console?
a. console.write()
b. console.output()
c. console.log()
d. console.writeHTML()
ANSWER: C
EXPLANATION: `console.log()` is the standard JavaScript method used to print information to the web browser's developer console for debugging.

12. Which JavaScript method is used to write into an alert box?
a. window.alertHTML()
b. window.alert()
c. window.alertBox()
d. window.alertContent()
ANSWER: B
EXPLANATION: `window.alert()` (or simply `alert()`) instructs the browser to display a dialog box with an optional message, and waits until the user dismisses it.

13. In HTML DOM, which of the following properties are read-only?
a. nodeName
b. nodeValue
c. nodeName and nodeValue
d. None of the previous
ANSWER: A
EXPLANATION: The `nodeName` property is strictly read-only (it returns the tag name like 'DIV' or '#text'). The `nodeValue` property is read-write for Text, Comment, and CDATA nodes.

14. Which is the correct syntax to declare a constant in JavaScript?
a. const constant_name;
b. constant name const;
c. constant name const = value;
d. const constant_name = value;
ANSWER: D
EXPLANATION: When declaring a constant using the `const` keyword, you must initialize it with a value at the time of declaration.

15. What will be the output of this code?
ANSWER: D
EXPLANATION: Attempting to reassign a new value to a constant variable declared with `const` throws a `TypeError: Assignment to constant variable.`
<script>
const VALUE=10;
VALUE=20;
</script>
a. 10
b. 20
c. ValueError
d. TypeError

16. Can we use a function as a variable value?
a. Yes
b. No
ANSWER: A
EXPLANATION: Yes. In JavaScript, functions are first-class citizens, meaning they can be assigned to variables, passed as arguments, and returned from other functions.

17. What will be the value of X?
ANSWER: B
EXPLANATION: JavaScript is case-sensitive. `X` (uppercase) is the global variable `10`; `x` (lowercase) is a separate `let` inside `myfunc`, which is never called. `document.write(X)` outputs `10`.
<script>
var X=10;
function myfunc()
{
let x=20;
}
document.write(X);
</script>
a. 20
b. 10
c. ValueError
d. TypeError

18. What is the output of the following JavaScript code?
ANSWER: A
EXPLANATION: JavaScript evaluates left to right: `10 + 20` is `30`, then `30 + "5"` coerces to string concatenation, giving `"305"`.
<script>
var a=10+20+"5";
document.getElementById("demo").innerHTML = a;
</script>
a. 305
b. 35
c. TypeError
d. ValueError

19. What will be the output of the following JavaScript code?
ANSWER: D
EXPLANATION: JavaScript only has one numerical type: `number`. It does not differentiate between integers and floats in the `typeof` evaluation.
<script>
var x = 1234;
document.getElementById("test").innerHTML = typeof(x);
</script>
a. int
b. float
c. long
d. number

20. Which is the correct syntax to access an object property in JavaScript?
a. objectName:propertyName
b. objectName[propertyName]
c. objectName["propertyName"]
d. None of the previous
ANSWER: C
EXPLANATION: Bracket notation with a string literal — `objectName["propertyName"]` — reads a property in JavaScript. Dot notation (`objectName.propertyName`) works too but is not listed. Option A (`objectName:propertyName`) is object-creation syntax, not access. Option B (`objectName[propertyName]`) only works when `propertyName` is a variable holding the key; without that context, C is correct.

21. What will be the output of the following JavaScript code?
ANSWER: D
EXPLANATION: The `reduce` function starts with an initial value of 2. It iterates through the array: 2+1=3, 3+2=5, 5+3=8, 8+4=12. Since 12 is not listed in options A, B, or C, the correct choice is D.
<script>
var numbers = [1, 2, 3, 4];
function myfunc(total, value) {
return total + value;
}
var sum = numbers.reduce(myfunc, 2);
</script>
a. 10
b. 7
c. 9
d. None of the previous

22. What will be the output of the following JavaScript code?
ANSWER: C
EXPLANATION: `reduce` subtracts left to right: `170 - 50 = 120`, then `120 - 25 = 95`.
<script>
var numbers = [170, 50, 25];
function myfunc(total, value) {
return total - value;
}
var x = numbers.reduce(myfunc);
document.write(x);
</script>
a. 100
b. 245
c. 95
d. None of the previous

23. "Expires: Tuesday, 17 Dec 2020 18:30 GMT" is a header denoting
a. request, time-based caching
b. response, content-based caching
c. request, content-type caching
d. response, time-based caching
ANSWER: D
EXPLANATION: The `Expires` header is an HTTP response header that contains the date/time after which the response is considered stale, denoting time-based caching.

24. What will be the output of the following JavaScript code?
ANSWER: B
EXPLANATION: `forEach` calls `myFunction` for each element. Each call adds the value to `result` (`result += value`), so the total is 10 + 20 + 30 = 60. The output is `Result: 60`.
<script>
const arr = [10, 20, 30];
let result = 0;
arr.forEach(myFunction);
document.write("Result: ", result);
function myFunction(value, index, array) {
result += value;
}
</script>
a. Result: 102030
b. Result: 60
c. Result: 10,20,30
d. ValueError

25. What will be the output of the following JavaScript code?
ANSWER: C
EXPLANATION: The `map` function creates a new array by executing the provided function on each element. It squares each value, returning `[100, 400, 900]`. When an array is coerced to a string for `document.write`, it joins elements with commas, yielding `Result: 100,400,900`.
<script>
const values = [10, 20, 30];
const result = values.map(myFunction);
document.write("Result: ", result);
function myFunction(value, index, array) {
return value * value;
}
</script>
a. Result: 10,20,30
b. Result: 10*10,20*20,30*30
c. Result: 100,400,900
d. Result: 100400900

26. Which function is used to serialize an object into a JSON string in JavaScript?
a. parse()
b. convert()
c. stringify()
d. None of the previous
ANSWER: C
EXPLANATION: The correct global object method is `JSON.stringify()`. (Option C specifies `stringify()`, which is the core method name).

27. In Python, ____ defines a block of statements.
a. Block
b. Loop
c. Indentation
d. None of the previous
ANSWER: C
EXPLANATION: Unlike languages that use curly braces `{}` to define scope, Python relies entirely on leading whitespace (indentation) to group blocks of statements.

28. What will be the output of this JavaScript code?
var numbers = [10, 20, 30];
function myFunction(value, index, array) {
return value > 10;
}
var y = numbers.find(myFunction);
document.write(y);
a. 20,30
b. [20,30]
c. 20
d. None of the previous
ANSWER: C
EXPLANATION: The `find()` method returns the value of the *first* element in the provided array that satisfies the testing function. The first value greater than 10 is 20.

29. What will be the output of the following JavaScript code?
ANSWER: B
EXPLANATION: After `delete cars.company`, `cars.company` is `undefined`. With `x = "color"`, `cars[x]` is `cars["color"]` → `"white"`, so the output is `undefined white`.
<script>
try {
const cars = {
company: "Honda",
color: "white"
};
var x = "color";
delete cars.company;
document.write(cars.company);
document.write(cars[x]);
} catch (err){
document.write(err.message);
}
</script>
a. Honda undefined
b. undefined white
c. undefined color
d. TypeError

30. What will be the output of the following JavaScript code?
ANSWER: D
EXPLANATION: The `eval()` function evaluates JavaScript code represented as a string. It accesses the scope variables `x` and `y`, calculating `10 * 20`, which is the number `200` (option d), not the string `"200"`.
<body>
<p id="demo"></p>
<script>
let x = 10;
let y = 20;
let text = "x * y";
let result = eval(text);
document.getElementById("demo").innerHTML = result;
</script>
</body>
</html>
a. x * y
b. "200"
c. ValueError
d. 200

31. What will be the output of this JavaScript code?
ANSWER: B
EXPLANATION: `filter()` returns an array, and `typeof` an array in JavaScript is `"object"`.
<script>
var numbers = [10, 20, 30];
function myFunction(value, index, array) {
return value > 10;
}
var y = numbers.filter(myFunction);
document.write(typeof(y));
</script>
a. array
b. object
c. number
d. None of the previous

32. What will be the output of this JavaScript code?
ANSWER: A
EXPLANATION: `lastIndexOf(10)` is `2`, `indexOf(30)` is `3`, and `2 + 3 = 5` (option a).
<script>
var numbers = [10, 20, 10, 30];
var y = numbers.lastIndexOf(10);
var z = numbers.indexOf(30);
document.write(y + z);
</script>
a. 5
b. 3
c. 2
d. None of the previous

33. What will be the output of the following JavaScript code snippet?
<script>
document.write(typeof(NaN));
</script>
ANSWER: B
EXPLANATION: In JavaScript, `typeof NaN` evaluates to `"number"` because `NaN` is a numeric value.
a. NaN
b. number
c. object
d. None of the previous

34. What will be the output of the following JavaScript code?
var quiz = [1, 2, 3];
var result = quiz.concat(6, 7, 8);
document.write(result);
a. 1,2,3,6,7,8
b. [1,2,3,6,7,8]
c. array
d. None of the previous
ANSWER: A
EXPLANATION: `concat` merges the array with the new elements, creating `[1, 2, 3, 6, 7, 8]`. When `document.write` coerces this array to a string for display, it outputs the elements separated by commas: `1,2,3,6,7,8`.

35. What will be the output of the following JavaScript code?
var a = 0;
for (a=0; a<5; a++);
document.write(a);
a. 01234
b. 0
c. Error
d. 5
ANSWER: D
EXPLANATION: The semicolon after `for (a=0; a<5; a++);` ends the loop with an empty body, so only `a` is incremented until it reaches 5. `document.write(a)` runs once afterward and prints `5`.

36. What will be the output of the following Python code?
newtuple = (1, 2, 3, 4)
(*k, ) = newtuple
print(k)
a. 1, 2, 3
b. [1, 2, 3, 4]
c. 2, 3, 4
d. None of the previous
ANSWER: B
EXPLANATION: The asterisk `*` unpacking operator gathers all elements from the tuple into a list. `k` becomes `[1, 2, 3, 4]`, matching option b.

37. What will be the output of the following Python code?
newtuple = (1, 2, 3, 4)
(x, *k, y) = newtuple
print(k)
a. 2, 3
b. [2, 3, 4]
c. [2, 3]
d. None of the previous
ANSWER: C
EXPLANATION: The unpacking syntax assigns the first element to `x` (1), the last element to `y` (4), and packs all remaining intermediate elements into a list `k`. Therefore, `k` is `[2, 3]`.

38. Which is the correct HTML statement to define the red color of the paragraph text?
a. <p style="color: #ff0000;">
b. <p style="color: red;">
c. Both a and b.
d. None of the previous
ANSWER: C
EXPLANATION: CSS allows color definition using predefined color names (like "red") or hexadecimal codes (like "#ff0000"). Both syntaxes are valid.

39. Which HTML tag is used to define highlighted text?
a. <highlight>
b. <mark>
c. <m>
d. None of the previous
ANSWER: B
EXPLANATION: The `<mark>` tag is the standard HTML5 element used to represent text that has been highlighted or marked for reference purposes.

40. Which is the correct HTML statement to display H2O in a paragraph?
a. <p>H<sup>2</sup>O</p>
b. <p>H<sub>2</sub>O</p>
c. <p>H<ins>2</ins>O</p>
d. <p>H<below>2</below>O</p>
ANSWER: B
EXPLANATION: The subscript 2 in H₂O uses `<sub>`. Option B — `<p>H<sub>2</sub>O</p>` — is correct. Option A wrongly uses `<sup>`, C uses `<ins>`, and D uses the invalid `<below>` tag.

41. Which is the correct HTML statement to display <del>Hello</del> <u>Hi</u> in a paragraph?
a. <p><del>Hello</del> <ins>Hi</ins></p>
b. <p><cut>Hello</cut> <ins>Hi</ins></p>
c. <p><del>Hello</del> <mark>Hi</mark></p>
d. None of the above
ANSWER: A
EXPLANATION: Deleted text uses `<del>` and inserted text uses `<ins>`. There is no standard `<cut>` element.

42. Which attribute specifies the path to the image?
a. href
b. src
c. link
d. All of the above
ANSWER: B
EXPLANATION: The `src` (source) attribute is used in the `<img>` tag to specify the URL or path to the image file.

43. Can we use class name with multiple HTML elements?
a. Yes
b. No
ANSWER: A
EXPLANATION: Yes. Unlike the `id` attribute, which must be unique per document, `class` attributes are specifically designed to be reused across multiple elements.

44. The ____ property is used in the positioning of the background image.
a. background-image
b. background-position
c. padding
d. All of the above
ANSWER: B
EXPLANATION: The `background-position` CSS property sets the initial position for a background image.

45. Which of the following selectors selects the specified elements of type E with a particular class value?
a. E-F
b. E.class
c. E#class
d. Class(E)
ANSWER: B
EXPLANATION: The CSS syntax for selecting an element `E` that possesses a specific class name is `E.classname`.

46. Which of the following selectors selects all elements of E that have the attribute (attr) that ends with a given (value)?
a. E[attr$=value]
b. E[attr=value]
c. E[attr*=value]
d. None of the above
ANSWER: A
EXPLANATION: The `$=` operator in a CSS attribute selector strictly matches elements whose attribute value ends with the specified string.

47. Which of the following selectors selects an element that has no children?
a. :nochild
b. :empty
c. :no-child
d. None of the above
ANSWER: B
EXPLANATION: The `:empty` pseudo-class represents any element that has no children (including text nodes).

48. What will be the output of the following Python code?
b = "Hello, World!"
print(b[-5:-2])
a. orl
b. or
c. Wor
d. None of the above
ANSWER: A
EXPLANATION: Negative indexing in Python starts from the end (-1 is '!'). The slice `[-5:-2]` starts at the 5th character from the end ('o') and stops before the 2nd character from the end ('d'). The characters are 'o', 'r', 'l', giving "orl".

49. What will be the output of the following Python code?
thistuple = (1, 2, 3, 3, 4, 5)
x = thistuple.index(3)
print(x)
a. 3
b. 4
c. 2
d. None of the previous
ANSWER: C
EXPLANATION: `index(3)` returns the index of the first `3`, which is `2` (0-based indexing).

50. If an application is running on Asynchronous Server Gateway Interface (ASGI), then it will handle client requests in a sequential manner?
a. True
b. False
ANSWER: B
EXPLANATION: False. ASGI is specifically designed to handle requests asynchronously and concurrently, unlike WSGI, which is synchronous/sequential.

51. Can you play audios in HTML?
a. Yes
b. No
ANSWER: A
EXPLANATION: Yes, HTML5 introduced the native `<audio>` element for embedding sound content.

52. What will be the output of the following Python code?
fruits = {"apple", "banana", "cherry"}
fruits.add("apple")
x = len(fruits)
print(x)
a. 4
b. 3
c. 2
d. None of the previous
ANSWER: B
EXPLANATION: Sets in Python do not allow duplicate values. Adding "apple" when it already exists has no effect. The length remains 3.

53. What will be the output of the following Python code?
car = {
"brand": "Ford",
"model": "Mustang",
"year": 1964
}
y = car.keys()
car["color"] = "red"
print(len(y))
a. 3
b. 6
c. 4
d. None of the previous
ANSWER: C
EXPLANATION: `dict.keys()` reflects live changes; after adding `color`, there are four keys.

54. To add items from another set to the current set we use ____ method, while to append items from another list to the current list we use ____ method.
a. Extend(), append()
b. Extend(), update()
c. Update(), extend()
d. Update(), append()
ANSWER: C
EXPLANATION: Sets use `update()`; lists use `extend()` when adding from another collection.

55. To make a copy of a dictionary, we can use ____ method.
a. copy()
b. dict()
c. a. and b.
d. makecopy()
ANSWER: C
EXPLANATION: Both `copy()` and `dict()` shallow-copy a dictionary, so both methods in options a and b are valid.

56. In CSS, "color:red" can be called as
a. Selector
b. Declaration
c. Rule
d. Value
ANSWER: B
EXPLANATION: In CSS terminology, the combination of a property (`color`) and its value (`red`) separated by a colon is formally known as a Declaration.

57. External CSS style has the highest priority
a. True
b. False
Inline > Internal > External > Browser Default
ANSWER: B
EXPLANATION: False. Inline styles (using the `style` attribute directly on an HTML element) have the highest specificity and priority, overriding external stylesheets.

58. The ____ HTTP request header specifies information about the software (browser) making the request.
a. Referrer
b. User-Agent
c. cookie
d. None of the above
ANSWER: B
EXPLANATION: The `User-Agent` string allows network protocol peers to identify the application type, operating system, software vendor, or software version of the requesting user agent.

59. What does {{ name }} this mean in Django Templates?
a. {{ name }} will be the output.
b. It will be displayed as name in HTML.
c. The name will be replaced with values of Python variable.
d. None of the above.
ANSWER: C
EXPLANATION: Double curly braces `{{ }}` constitute a template variable in Django; the engine interpolates this and outputs the value of the backend Python variable passed through the context.

60. What is the output of the following JavaScript snippet?
x = "20";
y = "10";
document.writeln(x+y, x-y);
a. 30, 10
b. 30, NAN
c. 2010, NAN
d. 2010, 10
ANSWER: D
EXPLANATION: With string operands, `+` concatenates to `2010`. The `-` operator coerces the strings to numbers, giving `10`.

61. Which of the following is the correct way of creating a hyperlink in HTML?
a. <a>www.google.com</a>
b. <a href="www.google.com" google</a>
c. <a href="www.google.com"> google</a>
d. <a link="www.google.com"> google </a>
ANSWER: C
EXPLANATION: The proper standard structure for a hyperlink utilizes the `<a>` (anchor) tag, assigning the URL destination to the `href` attribute, and wrapping the clickable text before the `</a>` closing tag.

62. What is the purpose of using div tags in HTML?
a. For creating different sections
b. For creating different styles
c. For adding headings.
d. For adding titles
ANSWER: A
EXPLANATION: The `<div>` tag is a generic block-level container used to divide the HTML document into distinct structural sections or divisions.

63. Which of the following is the correct way to add background color in HTML?
a. <body color="green">
b. <body bg-color="green">
c. <body style="background-color: green">
d. <body style="background-color: green;">
ANSWER: C
EXPLANATION: Inline CSS is the correct modern approach. You utilize the `style` attribute and correctly pair the `background-color` property with the value using a colon separator.

64. If you have the following JavaScript statement: const f = (a, b) => a + b + 5;
a. f(3,4) will return 7
b. f(4,5) will return 9
c. f(4,5) will return 14
d. Not a correct syntax for an arrow function
ANSWER: C
EXPLANATION: `const f = (a, b) => a + b + 5` is valid arrow-function syntax; `f(4, 5)` returns `4 + 5 + 5 = 14`.

65. To multiply an array of numbers (named myNumbers) with 4, the following is a correct syntax to do it:
a. myNumbers.map((value) => value * 4);
b. myNumbers.filter((value) => value * 4);
c. myNumbers.map(value * 4);
d. myNumbers.filter(value * 4);
ANSWER: A
EXPLANATION: The `map()` method transforms every element and requires a callback function. Option A supplies a valid arrow function.

66. To keep numbers that are less than 10 from an array of numbers (named myNumbers), the following is a correct syntax to do it:
a. myNumbers.map((value) => value < 10);
b. myNumbers.filter((value) => value < 10);
c. myNumbers.map(value < 10);
d. myNumbers.filter(value < 10);
ANSWER: B
EXPLANATION: The `filter()` method keeps elements that pass a callback test. Option B supplies a valid arrow function.

67. var colors = {1:"red", 2:"green", 3: "blue"} is one of the ways of defining an array in JavaScript?
a. True
b. False
ANSWER: B
EXPLANATION: This syntax is invalid. Arrays are defined using square brackets `[]` (e.g., `["red", "green", "blue"]`).

68. In the following statement: myNumbers = [1, 2, 3]; x = myNumbers.pop();
The removed item will be 1
a. True
b. False
ANSWER: B
EXPLANATION: The `pop()` method extracts and returns the *last* element of an array, which in this case is 3, not 1.

69. In the following statement: myNumbers = [1, 2, 3]; x = myNumbers.pop();
The removed item will be 3
a. True
b. False
ANSWER: A
EXPLANATION: The `pop()` function successfully removes the final index item from the array (3) and returns it.

70. In Python, the value of [] is False?
a. True
b. False
ANSWER: A
EXPLANATION: In Python, empty data structures such as an empty list `[]` inherently evaluate to a boolean value of `False`.

71. In Python, it is possible to use the set() constructor to create a set?
a. True
b. False
ANSWER: A
EXPLANATION: Yes, `set()` is the built-in constructor specifically designed for instantiating new set objects.

72. In Python, A set is a collection which is both unordered and indexed?
a. True
b. False
ANSWER: B
EXPLANATION: Sets are unordered and explicitly *unindexed*. You cannot retrieve elements from a set via index positions.

73. def f(**person):
print("Person's name is " + person["name"])
f(name="Ahmed", age=10)
The above statement will return:
a. Person's name is Ahmed
b. Person's name is Ahmed and Age is 10
c. Error as the function f doesn't take except one argument
d. Error as the function f doesn't take an argument called "name"
ANSWER: A
EXPLANATION: The `**kwargs` syntax `**person` packages the keyword arguments into a dictionary. The print statement successfully queries `person["name"]` to fetch "Ahmed".

74. y = lambda x : x / 20
print(y(100))
The above statement will return:
a. Error as there is no function called y
b. 100/20
c. 5
d. 5.0
ANSWER: D
EXPLANATION: Division in Python 3 returns a float: `100 / 20` is `5.0`.

75. In the following Python statements:
if 1 < 2:
print("One is less than Two!")
print("One is less than Two!")
a. The output will be: One is less than Two! One is less than Two!
b. The output will be: One is less than Two!
c. The output will be: One is less than Two!
One is less than Two!
d. The output will be an Error
ANSWER: D
EXPLANATION: Python requires an indented block under `if`. The second `print` is not indented into the `if` body, so this raises an `IndentationError`.

76. The output of the following statement in python is:
x = "My Lovely Bird!"
print(x[:7])
a. My Love
b. My Lovel
c. ly Bird!
d. y Bird!
ANSWER: A
EXPLANATION: The slice `[:7]` returns characters from index 0 up to (but not including) index 7. This extracts `M`, `y`, ` `, `L`, `o`, `v`, `e` resulting in `My Love`.

77. The output of the following statement in python is:
x = "My Lovely Bird!"
print(x[7:])
a. My Love
b. My Lovel
c. ly Bird!
d. y Bird!
ANSWER: C
EXPLANATION: `x[7:]` is a positive slice from index 7 to the end. Index 7 is the first `l` in `Lovely`, so the output is `ly Bird!`.

78. What will be the output of this JavaScript code?
const myArr = [1, 2, [3, [4, 5, 6], 7], 8];
const newArr = myArr.flat();
document.writeln(newArr);
a. 1,2,3,[4,5,6],7,8
b. 1,2,3,4,5,6,7,8
c. 1,2,[3,[4,5,6],7],8
d. None of the previous
ANSWER: B
EXPLANATION: With depth 1, `flat()` yields `[1, 2, 3, [4, 5, 6], 7, 8]`. `document.writeln` coerces the array to a string; nested arrays join with commas, producing `1,2,3,4,5,6,7,8`.

Based on the following HTML page, answer the following questions:

<!DOCTYPE html>
<html>
<head>
</head>
<body>
<h2 id="id1">Hello World</h2>
<p>Welcome</p>
<div>
<p>Paragraph 1.</p>
<p>Paragraph 2.</p>
<section><p>Paragraph 3.</p></section>
</div>
<p>Paragraph 4.</p>
<p>Paragraph 5.</p>
<script>
const my = document.getElementsByTagName("p");
document.getElementById("id1").innerHTML = my.length;
</script>
</body>
</html>

79. To get the value of the text node of the h2 element node, you can use
a. document.getElementById("id1").innerHTML
b. document.getElementById("id1").text;
c. document.getElementById("id1").nodeValue
d. none of the above
ANSWER: D
EXPLANATION: Text inside an element lives in a child text node. The correct approach is `document.getElementById("id1").firstChild.nodeValue`. `innerHTML` is not equivalent; `.text` is not a standard element property; and `nodeValue` on the element itself is `null`.

80. Based on the above html page, the content of h2 element will be presented as
a. Hello World
b. Hello World 6
c. 6
d. None of the above
ANSWER: C
EXPLANATION: The script sets `h2` innerHTML to `my.length`, the count of `<p>` elements (6).

81. The number of objects in my variable is
a. 4
b. 5
c. 6
d. 3
ANSWER: C
EXPLANATION: Six `<p>` elements exist in the page, so the collection length is 6.

82. By using this CSS selector div > p { color: red; }, how many p elements will be styled?
a. 3
b. 2
c. 5
d. 6
ANSWER: B
EXPLANATION: The child combinator `>` selects only direct `<p>` children of `<div>`: Paragraph 1 and Paragraph 2. Paragraph 3 is inside `<section>`, not a direct child of `<div>`.

83. By using this CSS selector div ~ p { color: blue; }, the number of p elements that will be styled is
a. 3
b. 2
c. 5
d. 6
ANSWER: B
EXPLANATION: The general sibling combinator `~` selects `<p>` siblings that follow `<div>` at the same level: Paragraph 4 and Paragraph 5.

84. Using document.getElementById("id1").nodeType will return
a. Element node
b. 1
c. Text node
d. 3
ANSWER: B
EXPLANATION: The `nodeType` property returns an integer. For an element node (like the `<h2>` targeted here), `nodeType` is `1`.

Based on the following HTML page, answer the following questions

<!DOCTYPE html>
<html>
<head>
<style>
a[target=_blank] {
background-color: yellow;
}
p:first-child { color: red; }
</style>
</head>
<body>
<h2>CSS Selector</h2>
<p>Hello I am Here: P1</p>
<p>Hello I am Here: Paragraph 2</p>
<p>Hello I am Here: P3</p>
<a href="https://www.w3schools.com">w3schools.com</a>
<a href="http://www.disney.com" target="_blank">disney.com</a>
<a href="http://www.wikipedia.org" target="_top">wikipedia.org</a>
</body>
</html>

85. In a[target=_blank], the [target=_blank] is called
a. attribute selector
b. anchor selector
c. pseudo element selector
d. none of the above
ANSWER: A
EXPLANATION: Square brackets `[]` encapsulate CSS attribute selectors, identifying components by the presence or explicit value of HTML attributes.

86. By using p:first-child { color:red; }, the number of elements that will be styled
a. 1
b. 2
c. 3
d. None of the above
ANSWER: D
EXPLANATION: `:first-child` matches only when an element is the first child of its parent. Under `<body>`, `<h2>` is the first child, not any `<p>`, so none match.

87. To change the background color of all <a> elements to be yellow, you can use
a. a[target] { background-color: yellow; }
b. a.all { background-color: yellow; }
c. a[target=_blank, target=_top] { background-color: yellow; }
d. a { background-color: yellow; }
ANSWER: D
EXPLANATION: Using just the plain element selector (`a`) targets all anchor tags ubiquitously across the document.

88. To change the color of <a> elements to yellow when the mouse moves over it, you can use
a. a:hover { background-color: yellow; }
b. a::hover { background-color: yellow; }
c. a:link { background-color: yellow; }
d. a:active { background-color: yellow; }
ANSWER: A
EXPLANATION: The `:hover` pseudo-class specifically manages state changes triggered by a user placing their pointing device over an element.

89. href is a/an ____ while "_blank" is a ____
a. value, attribute
b. link, value
c. attribute, value
d. link, text node
ANSWER: C
EXPLANATION: In HTML syntax rules, `href` acts as the property name (the attribute), whereas strings enclosed in quotes like `"_blank"` operate as assigned values.

90. The hyperlink <a href="https://w3schools.com">...</a> is an example of a/an ____ URL, while <a href="Page2.html">...</a> is an example of a/an ____ URL.
a. Absolute, relative
b. Relative, absolute
c. Relative, Relative
d. None of the above
ANSWER: A
EXPLANATION: An Absolute URL includes the entire operational path (protocol and domain). A Relative URL merely provides the path correlating to the currently loaded page's domain/directory.

End of Question
Good luck!
